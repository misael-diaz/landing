# landing
landing page

## Developer Tools

Setting up breakpoints with chromium developer tools is the best way to debug the code, see this article for a quick starting on [debugging](https://developer.chrome.com/docs/devtools/javascript#event-breakpoint).

## CORS

This [MDN resource](https://developer.mozilla.org/en-US/docs/Web/HTTP/Guides/CORS/Errors/CORSRequestNotHttp?utm_source=devtools&utm_medium=firefox-cors-errors&utm_campaign=default) explains why scripts will trigger CORS errors when testing the page locally (without a web server, that is not via the `http` protocol but `file`).
